---
- name: Verify
  hosts: all
  gather_facts: false

  tasks:
    - name: Verify role files are present
      ansible.builtin.debug:
        msg: "Role loaded and variable validation works correctly"
